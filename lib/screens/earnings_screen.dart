import 'package:flutter/material.dart';

class EarningsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Earnings'),
      ),
      body: Center(
        child: Text('Earnings Screen Content'),
      ),
    );
  }
}
